<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>Planning App – iPhone 16 Portrait</title>
  <style>
    :root { color-scheme: light dark; }
    * { box-sizing: border-box; }
    html, body { height: 100%; margin: 0; }
    body {
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: #0f172a;
      display: grid; place-items: center;
    }
    /* 아이폰16 논리 뷰포트(393×852) 프레임 */
    .device {
      width: 393px;            /* 논리 너비 */
      height: 852px;           /* 논리 높이 */
      max-width: 100vw;        /* 브라우저 축소 시 대응 */
      max-height: 100dvh;
      aspect-ratio: 393 / 852;
      border-radius: 36px;
      overflow: hidden;
      background: #ffffff;
      box-shadow: 0 24px 80px rgba(0,0,0,.55);
    }
    /* 안전영역 적용 (좌우 포함) */
    .app {
      height: 100%;
      padding-top:    calc(16px + env(safe-area-inset-top));
      padding-bottom: calc(16px + env(safe-area-inset-bottom));
      padding-left:   calc(16px + env(safe-area-inset-left));
      padding-right:  calc(16px + env(safe-area-inset-right));
    }
    .header {
      position: sticky; top: 0;
      padding-bottom: 8px;
      background: inherit;
    }
    /* 헤더 이미지 스타일 */
    .header-image {
      width: 100%;
      aspect-ratio: 393 / 220;   /* 필요시 393/200 등으로 더 낮게 조절 가능 */
      object-fit: cover;         /* 넘치는 부분은 잘라 보여줌 */
      display: block;
      border-radius: 16px;
      margin: 8px 0 12px;        /* 위아래 간격 */
    }
    .card {
      border: 1px solid rgba(0,0,0,.08);
      border-radius: 16px;
      padding: 16px;
      margin: 12px 0;
      box-shadow: 0 6px 18px rgba(0,0,0,.06);
    }
    .button {
      display: inline-block;
      padding: 12px 16px;
      border-radius: 12px;
      background: #5b21b6;
      color: #fff;
      text-decoration: none;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <!-- role/aria-label은 메인 랜드마크 중복이라 제거해도 무방 -->
  <main class="device">
    <section class="app">
      <div class="header">
        <!-- Plan 제거하고 이미지를 헤더 안에 넣음 -->
        <img
          class="header-image"
          src="https://i.imgur.com/LPJLqPG.png"
          alt="오늘 일정 일러스트"
          loading="eager"
          decoding="async"
        >
      </div>
      
      <div class="card">
        <strong>오늘의 할 일</strong>
        <p style="margin:.5rem 0 1rem">
          UI는 <b>393×852(CSS px)</b> 기준으로 설계하고, 이미지 에셋은 @3x(= <b>1179×2556</b>)로 준비하세요.
        </p>
        <a class="button" href="#">새 작업 추가</a>
      </div>
    </section>
  </main>
</body>
</html>
