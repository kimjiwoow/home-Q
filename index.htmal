<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>Planning App – iPhone 16 Portrait</title>
  <style>
    /* 전역 리셋 */
    * { box-sizing: border-box; }
    html, body { height: 100%; margin: 0; }
    /* 페이지 배경 + 가운데 정렬 */
    body {
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: #0f172a;
      display: grid;
      place-items: center;
    }
    /* iPhone 16 논리 뷰포트(393×852) 프레임 */
    .device {
      width: 393px;
      height: 852px;
      max-width: 100vw;
      max-height: 100dvh;      /* 모바일 주소창 높이 변동 대응 */
      border-radius: 36px;
      overflow: hidden;
      background: #ffffff;
      box-shadow: 0 24px 80px rgba(0,0,0,.55);
      aspect-ratio: 393 / 852; /* 창이 작아질 때 비율 유지 */
    }
    /* 앱 캔버스 */
    .app {
      height: 100%;
      padding: 16px;           /* 필요시 safe-area 패딩 추가 가능 */
    }
    /* 히어로 이미지: 프레임 너비에 맞추고, 세로는 고정 비율로 자연스럽게 크롭 */
    .hero {
      width: 100%;
      aspect-ratio: 393 / 220; /* 더 얕게 보이게 하려면 393/200 등으로 조절 */
      object-fit: cover;
      display: block;
      border-radius: 16px;
      margin-bottom: 12px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
    /* 카드 컴포넌트 */
    .card {
      border: 1px solid rgba(0,0,0,.08);
      border-radius: 16px;
      padding: 16px;
      margin: 12px 0;
      box-shadow: 0 6px 18px rgba(0,0,0,.06);
    }
    /* 버튼 */
    .button {
      display: inline-block;
      padding: 12px 16px;
      border-radius: 12px;
      background: #5b21b6;
      color: #fff;
      text-decoration: none;
      font-weight: 600;
      transition: background-color 0.2s ease;
    }
    .button:hover {
      background: #4c1d95;
    }
    /* 플레이스홀더 이미지 스타일 */
    .hero-placeholder {
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 18px;
      font-weight: 600;
      text-align: center;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
  </style>
</head>
<body>
  <main class="device">
    <section class="app">
      <!-- imgur 히어로 이미지 -->
      <img
        class="hero"
        src="https://i.imgur.com/LPJLqPG.png"
        alt="Planning App Hero Image"
        loading="eager"
        decoding="async"
        onerror="this.style.display='none'; document.getElementById('heroFallback').style.display='flex';"
      >
      <!-- 이미지 로드 실패 시 백업 -->
      <div class="hero hero-placeholder" id="heroFallback" style="display: none;">
        Planning App
      </div>
      
      <div class="card">
        <strong>오늘의 할 일</strong>
        <p style="margin:.5rem 0 1rem">
          UI는 <b>393×852(CSS px)</b> 기준으로 설계하고, 이미지 에셋은 @3x(= <b>1179×2556</b>)로 준비하세요.
        </p>
        <a class="button" href="#" onclick="addNewTask()">새 작업 추가</a>
      </div>
      
      <div class="card">
        <strong>이미지 업로드 해결방법</strong>
        <ul style="margin: 0.5rem 0; padding-left: 1.2rem;">
          <li>GitHub에서 <code>images</code> 폴더를 만들고 이미지 업로드</li>
          <li>절대 경로 사용: <code>/repository-name/images/hero.jpg</code></li>
          <li>또는 외부 이미지 URL 사용</li>
        </ul>
      </div>
    </section>
  </main>

  <script>
    function addNewTask() {
      alert('새 작업 추가 기능 - 실제 앱에서는 모달이나 새 페이지로 이동');
    }
  </script>
</body>
</html>
